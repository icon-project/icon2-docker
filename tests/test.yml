schemaVersion: '2.0.0'
commandTests:
  - name: "curl package installation"
    setup: [["bash", "-c", "while [ 0 ]; do echo 'sleep';sleep 1;done"]]
#    setup: [["/init"]]
    command: "ps"
    args: ["-a"]
#    expectedOutput: ["/usr/bin/curl"]
